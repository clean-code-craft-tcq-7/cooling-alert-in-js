import { alertTemperatureForCoolingType } from './thresholds.mjs';

// ----------- STEP 6 code starts -------

// export function batteryDataToAction(batteryData, actuators) {
//     // Two stage chain to classify battery data and take action
//     if (batteryData.temperatureInC > alertTemperatureForCoolingType(batteryData.thermalManagementType)) {
//         actuators.emailSender('manager@battery.com', 'High Battery Temperature Alert', 'The battery temperature is above 60°C', 'noreply@battery.com');
//     }
// }

// ----------- STEP 6 code ends -------
